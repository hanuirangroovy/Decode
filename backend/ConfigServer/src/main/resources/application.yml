server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
#        native:
#          search-locations: file://${user.home}/ssafy2/config-yml
        git:
          uri: https://github.com/cheery72/config-yml.git

